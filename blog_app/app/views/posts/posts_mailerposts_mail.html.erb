<h1>Email-рассылка</h1>

<table>
  <thead>
    <tr>
      <th>Created at</th>
      <th>Title</th>
      <th>Content</th>
    </tr>
  </thead>

  <tbody>
    <% @posts.sort{|x,y| y.created_at <=> x.created_at}.each do |post| %>
      <tr>
        <td><%= post.created_at %></td>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
